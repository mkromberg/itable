 r←tbl(cols Select)query
⍝ Select from inverted table format

⍝ tbl contains:
⍝    name:     name of the table
⍝    columns:  column names
⍝    data:     inverted table data
⍝    key:      key column

⍝ query can be:
⍝    a single integer:
